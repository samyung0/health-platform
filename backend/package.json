{
  "name": "backend",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "NODE_ENV=production bun run src/app.ts",
    "dev": "NODE_ENV=development bun run --hot src/app.ts",
    "typecheck": "tsc --noEmit",
    "db:generate": "drizzle-kit generate --config drizzle.dev.config.ts",
    "db:migrate": "drizzle-kit migrate --config drizzle.dev.config.ts",
    "db:drop": "drizzle-kit drop --config drizzle.dev.config.ts",
    "db:generate:prod": "drizzle-kit generate --config drizzle.prod.config.ts",
    "db:migrate:prod": "drizzle-kit migrate --config drizzle.prod.config.ts",
    "db:drop:prod": "drizzle-kit drop --config drizzle.prod.config.ts",
    "db:seed": "NODE_ENV=development bun run src/scripts/seed.ts",
    "db:seedbase": "NODE_ENV=development bun run src/scripts/baseData.ts",
    "db:dropdata": "NODE_ENV=development bun run src/scripts/drop.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/pg": "^8.15.5",
    "drizzle-kit": "^0.31.4",
    "tsx": "^4.20.5"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.3",
    "better-auth": "^1.3.11",
    "dotenv": "^17.2.2",
    "drizzle-orm": "^0.44.5",
    "drizzle-seed": "^0.3.1",
    "hono": "^4.9.7",
    "pg": "^8.16.3",
    "uuid": "^13.0.0",
    "zod": "^4.1.9"
  }
}